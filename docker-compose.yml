version: '3.8'
services:
  cli:
    build: .
    stdin_open: true
    tty: true
    env_file: .env
    volumes:
      - ./data:/app/data

  dashboard:
    build: .
    command: node src/server.js
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
